export interface InputFormItemProps {
    title: string,
    type: string
}

export interface taskItemProps {
    id: number,
    time_stamp: Date,
    name: string,
    doing: boolean,
    editing: boolean
}
